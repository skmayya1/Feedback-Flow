export // For example, the `Data` interface should resemble this structure:
interface Data {
    avg_rating: number;
    email: string;
    name: string;
    image: string;
    url: string;
    id: string;
}
