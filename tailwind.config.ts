import type { Config } from "tailwindcss";

const config: Config = {
  content: [
    './src/**/*.{js,jsx,ts,tsx}', // Update these paths as needed
  ],
  theme: {
    extend: {},
  },
  plugins: [],
};
export default config;
